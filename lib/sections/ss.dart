import 'package:flutter/material.dart';

class SSsec extends StatelessWidget {
  const SSsec({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 400, color: Colors.blue);
  }
}
